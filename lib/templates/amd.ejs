define("<%-name -%>", [<%-dependencies.map(function(dep) {
  return "\"" + dep + "\"";
}).join(", ") %>], function(require) {

    var __dirname = "<%-__dirname %>",
    __filename    = "<%-__filename %>",
    module        = { exports: {} },
    exports       = module.exports;

    <%-content %>

    return module;
});