define("<%-__filename -%>", [<%-dependencies.map(function(dep) {
  return "\"" + dep + "\"";
}).join(", ") %>], function() {

    var __dirname = "<%-__dirname %>",
    __filename    = "<%-__filename %>",
    self          = this,
    require       = function() {
      return self.require.apply(self, arguments);
    };

    <%-content %>

    return module;
});